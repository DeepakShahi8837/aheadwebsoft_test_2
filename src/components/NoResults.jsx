

const NoResults = () => (
  <div className="text-center py-20 text-xl font-semibold text-gray-500">
    No properties found for the selected region or keyword.
  </div>
);

export default NoResults;
